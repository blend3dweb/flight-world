const {chromium}=require('C:/Users/sva/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict');
const out=path.join(__dirname,'stages','12-performance-refinement');
(async()=>{const browser=await chromium.launch({channel:'chrome',headless:false,args:['--force-high-performance-gpu','--use-webgpu-power-preference=high-performance','--window-size=1940,1180']});try{const results=[];
 for(const variant of [{name:'normal'},{name:'sparse-reflection',interval:100000}]){const page=await browser.newPage({viewport:{width:1920,height:1080}}),errors=[];page.on('pageerror',e=>errors.push(e.message));page.on('console',m=>{if(m.type()==='error')errors.push(m.text());});await page.goto('http://127.0.0.1:8765/webgpu/index.html?test=1');await page.bringToFront();await page.waitForFunction(()=>window.flightReady,null,{timeout:120000});
  await page.evaluate(()=>{flight.startFlight('city');flight.setEnvironment({hour:10,cycle:false,weather:'sun',autoWeather:false},{immediate:true});});await page.waitForTimeout(8000);await page.screenshot({path:path.join(out,`transition-${variant.name}-day.png`)});
  await page.evaluate(v=>{flight.startFlight('city');flight.setEnvironment({hour:0,cycle:false,weather:'sun',autoWeather:false},{immediate:true});flight.setView('external');flight.setOrbit({auto:true,distance:30});if(v.interval)flight.inspect().water.configure({interval:v.interval});},variant);
  const sample=await page.evaluate(async()=>{await new Promise(r=>setTimeout(r,8000));const times=[];await new Promise(resolve=>{const start=performance.now();let last=start;function tick(now){times.push(now-last);last=now;if(now-start<6000)requestAnimationFrame(tick);else resolve();}requestAnimationFrame(tick);});times.shift();const ordered=[...times].sort((a,b)=>a-b),pick=q=>ordered[Math.min(ordered.length-1,Math.floor(ordered.length*q))];return {fps:1000/(times.reduce((a,b)=>a+b,0)/times.length),median:pick(.5),p95:pick(.95),p99:pick(.99),max:ordered.at(-1),state:flight.getState()};});results.push({name:variant.name,...sample,errors});console.log(JSON.stringify(results.at(-1)));await page.close();
 }
 assert.ok(results.every(r=>r.errors.length===0));fs.writeFileSync(path.join(out,'transition-profile.json'),JSON.stringify(results,null,2));
 }finally{await browser.close();}})().catch(e=>{console.error(e);process.exit(1)});
